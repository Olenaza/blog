{% if access %}

    <a href = "{{ urlEdit is defined ? urlEdit : path('post_edit', {'slug': post.slug}) }}" class="btn btn-info">Змінити</a>

    <a href = "{{ urlDelete is defined ? urlDelete : path('post_delete', {'slug': post.slug}) }}" class="btn btn-danger">Видалити</a>

{% endif %}
